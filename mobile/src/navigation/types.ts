// Shared navigation param lists — re-exported from App.tsx definitions.
// Move-only refactor (plan 034): types are byte-identical to their source.

export type {
  AuthStackParamList,
  MainTabParamList,
  CargoStackParamList,
  ShipmentStackParamList,
  DriverStackParamList,
  RootStackParamList,
} from '../../App';
