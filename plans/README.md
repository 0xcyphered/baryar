# Implementation Plans

Generated by the improve skill on 2026-09-01 (plans 001-003). Plans 004-008 added 2026-09-02 for mobile UI/UX overhaul. Plan 009 added 2026-09-02 for Google Maps-style redesign. Plan 010 added 2026-09-02 as the first Baryar platform slice (product spec: `amin-tajeran-features-roadmap-v5.md`). Plan 011 added 2026-09-03 for Phase 1 Mongoose models. Plan 012 added 2026-09-03 to close RFP coverage gaps in `resources/features-roadmap.md` (source PDF: `resources/RFP.pdf`). Plan 013 added 2026-09-03 to align 011 `Cargo` with V6 five transport modes. Plan 014 added 2026-09-03 for Phase 1 phone OTP register/login (SMS gateway still Phase 2). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Complete Iran Map Feature Set — GPS Error Handling, Accuracy Radius, Numbered Markers, Straight-Line Polylines, Floating Summary Card | P1 | M | — | DONE (executed + reviewed + merged 2026-09-01) |
| 002  | Wrap the Vite React Map App in Capacitor iOS/Android Shells | P1 | L | — | REVERTED (executed, reviewed, merged, then reverted 2026-09-01 — operator pivoted to React Native) |
| 003  | React Native (Expo) Version of the Iran Map App for iOS and Android | P1 | XL | — (supersedes the reverted 002) | DONE (executed + reviewed 2026-09-01) |
| 004  | Safe-Area-Aware Layout for Control Panel, Toast, Mode Toggle, and Summary Card | P1 | M | — | DONE (executed 2026-09-02) |
| 005  | Add Gesture Dismissal and Backdrop to Control Panel | P1 | M | 004 | DONE (executed 2026-09-02) |
| 006  | Add Undo Waypoint and Tap-to-Remove in Measure Mode | P1 | M | 004 | DONE (executed 2026-09-02) |
| 007  | Add Compass / Reset-North Button to Map | P2 | S | — (ideally after 004) | DONE (executed 2026-09-02) |
| 008  | Add Haptic Feedback to All Touch Interactions | P2 | S | — | DONE (executed 2026-09-02) |
| 009  | Redesign Mobile UI to Match Google Maps Layout and UX | P1 | L | — (supersedes 004-005) | DONE (executed 2026-09-02) |
| 010  | Scaffold the Baryar Express + MongoDB API | P1 | M | — | DONE (executed 2026-09-02) |
| 011  | Add Phase 1 Baryar Mongoose models | P1 | L | 010 | DONE (executed 2026-09-03) |
| 012  | Close RFP gaps in `resources/features-roadmap.md` | P1 | M | — | DONE (executed 2026-09-03) |
| 013  | Add `Cargo.transportMode` for V6 land/sea/air/rail/multimodal cargo | P1 | S | 011 | DONE (executed 2026-09-03) |
| 014  | Add phone OTP register/login against the 011 User model | P1 | M | 011 | DONE (executed by pipeline) |
| 015  | Add cargo-owner draft CRUD REST API on the 011 Cargo model | P1 | M | 014 | TODO |

## Dependency notes

- 005 depends on 004 because it uses the same inset-based layout values (e.g. `CARD_BOTTOM`, `FABS_BOTTOM`) for correct positioning.
- 006 depends on 004 because the undo FAB stacking must be consistent with the bottom offset calculations.
- 007 and 008 are independent of all other plans — they add new features without changing layout.
- Recommended map-era execution order (historical): 004 → 005 → 006 → 007 → 008 → 009.
- 010 has no code dependency on 001–009. It starts the logistics platform next to the existing map apps. Do not edit `mobile/` or `webapp/` in 010.
- 011 depends on 010 because it adds Mongoose models inside the 010 `backend/` tree. Do not add routes, auth, or mobile wiring in 011.
- 012 is docs-only. It does not block 011 (already DONE). Later OTP and cargo-CRUD slices must read the updated modes + OTP bullets in `resources/features-roadmap.md`.
- 013 depends on 011 (`Cargo.js` exists) and on the V6 five-mode bullets. Live checklist is `resources/features-roadmap.md` (V6; `002d41c` renamed it from `features-roadmap-main.md`). Schema-only; no routes.
- 014 depends on 011 (`User.phone` + `phoneVerifiedAt`). It does **not** depend on 013. V6 parked the SMS OTP *gateway* in Phase 2 §9; 014 uses a pluggable sender (dev log + `OTP_FIXED_CODE` for tests) and HS256 JWT in the Authorization header. No cookies, no mobile wiring, no cargo routes.
- 015 depends on 014 (`auth` middleware + Bearer JWT). Owner-scoped CRUD over the 011 `Cargo` model with publish/cancel status exits — no model changes, no matching/offers (017), no shipments (018), no admin (019).

## MVP slice queue (automation source of truth)

This section is the canonical "next plan" queue for the mapapp MVP build pipeline (a cron agent runs every ~10 minutes: if a plan with status TODO exists it executes the lowest-numbered one; otherwise it writes the first queue row that has no plan file yet). Product source: `resources/features-roadmap.md` (V6 Phase 1). RFP: `resources/RFP.pdf`. Write **one plan at a time**. Each slice = exactly one plan file in `plans/` with the next free number. Build order is locked: backend API → admin webapp → mobile app → final test/debug pass.

| Next plan | Scope summary (the plan description) | Depends on |
|-----------|--------------------------------------|------------|
| 014 phone OTP auth | Executed by pipeline (see `plans/014-phone-otp-auth.md`, DONE). | 011 |
| 015 Cargo draft CRUD API (→ 015) | Phase 1 §2: cargo-owner-only REST for the Cargo registry — create/read/list/update/delete draft cargoes with `transportMode` (013 enum), physical dimensions (weight/volume/size), special characteristics (hazardous/fragile/refrigerated), pickup + delivery timing, and origin/destination GeoJSON points accepted as JSON body (no map wiring yet). JWT auth from 014 + `cargo_owner` role checks. No matching, no offers, no admin. | 014 |
| 016 Driver profile + vehicle + documents API | Phase 1 §3: driver onboarding REST — complete/update DriverProfile, register/manage Vehicles (road asset: type, plate, load capacity), and upload Document stubs (vehicle license, driver license, national/professional cards) storing metadata + status `pending` only (no object store yet). JWT + `driver` role. No matching. | 014 |
| 017 Matching + offers API | Phase 1 §2/§3: drivers list open cargoes matching their vehicle type + origin proximity; drivers submit Offer price bids on open cargo; owners list incoming offers for their cargo and select a provider (award). Offer statuses (pending/accepted/rejected) and Cargo status transitions updated accordingly. No GPS. | 015, 016 |
| 018 Shipment lifecycle + events + in-app notifications API | Phase 1 §4/§6: awarding a cargo creates a Shipment; driver manages trip status transitions (started / at_customs / completed + custom checkpoints via ShipmentEvent); status history endpoint per shipment; new Notification collection plus lifecycle-triggered in-app notification records (no push/SMS provider integration). Owner tracking/status endpoints. | 017 |
| 019 Admin backend API | Phase 1 §5: admin-guarded REST — list/manage users and drivers (block/unblock), cargo database oversight (edit/cancel any cargo), transport requests + active trips overview, driver document verification approve/reject, SystemSettings model + get/put endpoints, RBAC via `admin` role guard middleware. | 014, 016 |
| 020 Admin webapp shell | New `admin/` Vite React TS app (do NOT break the `webapp/` map demo): phone OTP login for admin users (JWT bearer in memory/localStorage), app layout + sidebar shell, typed API client, auth guards. | 019 |
| 021 Admin users/cargo/trips pages | Admin screens against the 019 API: users table + detail + block, drivers table, cargo database table + edit/cancel, transport requests overview, active trips monitor. | 020 |
| 022 Admin verification + settings pages | Document verification queue (approve/reject with reasons), system settings form, and basic role management UI against the 019 API. | 020 |
| 023 Mobile auth + API client | Expo: OTP request/verify screens, secure token storage, typed API client, auth state + logout. No cargo/driver screens yet. | 014 |
| 024 Mobile cargo-owner flow | Create cargo using the existing MapLibre origin/destination pickers + draft list/edit + shipment status tracking + offers list/award screens against the 015/017/018 APIs. | 023, 015 |
| 025 Mobile driver flow | Matching open-cargo list, offer submission, active trip status updates + checkpoint logging against the 016/017/018 APIs. | 023, 016 |
| 026 Final MVP test & debug pass | The operator's "test and debug at the end" slice: fix mongodb-memory-server for this WSL machine, run the full backend Jest suite, lint/typecheck/build webapp + admin + mobile, fix every failure found, and produce a written health summary. No new features. | 015–025 |

Do not collapse those into 010 or 011. Phase 2 (live GPS stream, payments, KYC, Telegram, companies, ratings) stays parked until Phase 1 booking works.

## Findings considered and rejected

- **RTL panel direction**: The panel opens from the left edge in an RTL app. In RTL, the left edge is the "end" side — this is actually correct for a navigation drawer. The panel toggle uses `chevron-forward`/`chevron-back` icons which adapt to RTL. No fix needed.
- **Bottom sheet migration**: The control panel is a full-height sidebar. The mobile-native pattern would be a bottom sheet (half/full sheet). This is addressed by plan 009 (Google Maps-style redesign) which replaces the sidebar with @gorhom/bottom-sheet.
- **Persistent GPS tracking**: The current GPS is one-shot. Adding `watchPositionAsync` with a toggle is a feature enhancement, not a UI fix. Noted as future work.
- **Neshan API migration**: Replacing Nominatim/OSRM with Neshan for better Iran coverage. Out of scope — current free APIs work. Noted in plan 001 index.
- **Test infrastructure**: No Jest/Vitest configured. Adding tests is a separate initiative. Verification in these plans is via lint + typecheck + manual.
- **Dark mode support**: Significant effort for a color-themed app. Deferred to future work.
- **Orientation lock**: Locking to portrait is common for map apps but not strictly necessary since the layout already works in both orientations. Low priority.
- **Concurrent Promise.all for OSRM routes**: Rejected per plan 001 notes — OSRM public demo server 429s concurrent requests.
- **Stale closure in SearchBar debounce**: The `handleSearch` callback has `[]` deps but receives `value` as a parameter from `onChangeText`, so it's not actually stale. The search works correctly.
- **Phase 2 collections in 011**: Company, Fleet, Staff, Rating, Payment, VehiclePosition, Notification, AuditLog, SystemSettings. The RFP lists them, but Phase 1 booking does not need them. Adding them now would freeze field names before the booking flow exists.
- **Email-first User identity**: Rejected. Iran-first identity is phone (`User.phone` unique). Email is optional profile text. No Firebase uid.
- **Embedding GPS points on Shipment / ShipmentEvent**: Rejected. Live GPS is Phase 2; 011 only allows an optional GeoJSON point on a discrete event (checkpoint / customs).
- **Password / otpHash on User in 011**: Rejected. Auth is the next slice; 011 only reserves `phone` + `phoneVerifiedAt`.
- **OTP / password columns in 013**: Rejected. V6 moved SMS Phone OTP to Phase 2 §9. 013 only adds `Cargo.transportMode`.
- **SMS gateway / Kavenegar / Twilio in 014**: Rejected. V6 Phase 2 §9. 014's sender logs in dev (with code) and uses `OTP_FIXED_CODE` in test; production persists the challenge and logs phone only, no SMS SDK.
- **Cookies + CORS lock-down in 014**: Rejected. 010 left CORS `origin: true` until cookies exist. 014 uses HTTP Bearer JWT so Expo can store a token later without a cookie domain.
- **otpHash / password on User in 014**: Rejected. Challenges live in `OtpChallenge`; User keeps `phone` + `phoneVerifiedAt` only.
- **Separate /register vs /login in 014**: Rejected. Same two endpoints (`request-otp` / `verify-otp`) upsert the cargo_owner user. Profile PATCH is a later slice.
- **Returning the OTP in the JSON body for tests in 014**: Rejected. Tests set `OTP_FIXED_CODE=123456`.
- **Refresh tokens / logout denylist / Redis in 014**: Rejected. One HS256 JWT, 7d. Fail closed if `JWT_SECRET` is missing.
- **Per-mode cargo spec columns in 013** (`containerNumber`, `imoNumber`, `awbNumber`, `wagonId`, Mixed `modeDetails`): Rejected. V6 names five register/manage surfaces; SRS locks extra attributes. Discriminator first.
- **Vehicle types for sea/air/rail in 013**: Rejected. `Vehicle` is a road asset (plate + `vehicleType`). Cargo mode lives on `Cargo`, not `Vehicle`.
- **Phase 2 collections in 013**: Same as 011 — Company, Fleet, Rating, Payment, GPS, Notification, AuditLog stay parked.
- **Copying RFP §5/§8/§9 (SOW, Play Store publish, NDA, SLA) into the feature list (012)**: rejected — contractor process, not app features.
- **Moving GPS / companies / payments into Phase 1 because the RFP lists them in 4.2 (012)**: rejected — RFP 4.6 allows phasing; existing V5 split stays. 012 only ensures they remain named (already true in Phase 2).
