# Implementation Plans

Generated by the improve skill on 2026-09-01 (plans 001-003). Plans 004-008 added 2026-09-02 for mobile UI/UX overhaul. Plan 009 added 2026-09-02 for Google Maps-style redesign. Plan 010 added 2026-09-02 as the first Baryar platform slice (product spec: `amin-tajeran-features-roadmap-v5.md`). Plan 011 added 2026-09-03 for Phase 1 Mongoose models. Plan 012 added 2026-09-03 to close RFP coverage gaps in `resources/features-roadmap.md` (source PDF: `resources/RFP.pdf`). Plan 013 added 2026-09-03 to align 011 `Cargo` with V6 five transport modes. Plan 014 added 2026-09-03 for Phase 1 phone OTP register/login (SMS gateway still Phase 2). Plan 016 added 2026-09-03 for Phase 1 §3 driver onboarding (driver profile, vehicles, document stubs). Plan 020 added 2026-09-04 for admin webapp shell (OTP login, sidebar layout, API client, auth guards). Plan 021 added 2026-09-04 for admin data pages (users, cargo, drivers, trips). Plan 022 added 2026-09-04 for admin document verification + settings pages. Plans 027-030 added 2026-09-05 after a V6 roadmap vs backend coverage audit (profile PATCH, matching filters, offer notifications + settings enforcement, local document upload). Plans 031-035 added 2026-09-05 after the MVP code review (188 tests green, backend 6/10, admin/mobile 7/10): 031-033 wire the mobile/admin clients to the 027-030 APIs, 034 extracts duplicated mobile constants + adds typed navigation (review #14/#17), 035 fixes the backend review's security/correctness findings (CORS #1, JWT pin #3, auth crash #6, error handler #7, bootstrap #9, N+1 #11, lean #17). Plans 036-039 added 2026-09-05 after a V6 Phase 1 (MVP) roadmap vs live-backend audit at `1232e61` (skip security; focus clean/reusable code + tests). 027-034 are DONE; 035 stays DEFERRED. Plan 041 added 2026-09-07 for user-mode §1 request submit flow clarity (first of the two post-040 mobile UX slices per the operator directive; plan dry-run-validated). Plans 049-052 added 2026-09-08 after a second V6 Phase 1 backend coverage delta at `baaa248` (skip security; clean/reusable code + tests): driver verification gate, admin public* serializers, matching query unification, leftover helper/multer/coverage. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Complete Iran Map Feature Set — GPS Error Handling, Accuracy Radius, Numbered Markers, Straight-Line Polylines, Floating Summary Card | P1 | M | — | DONE (executed + reviewed + merged 2026-09-01) |
| 002  | Wrap the Vite React Map App in Capacitor iOS/Android Shells | P1 | L | — | REVERTED (executed, reviewed, merged, then reverted 2026-09-01 — operator pivoted to React Native) |
| 003  | React Native (Expo) Version of the Iran Map App for iOS and Android | P1 | XL | — (supersedes the reverted 002) | DONE (executed + reviewed 2026-09-01) |
| 004  | Safe-Area-Aware Layout for Control Panel, Toast, Mode Toggle, and Summary Card | P1 | M | — | DONE (executed 2026-09-02) |
| 005  | Add Gesture Dismissal and Backdrop to Control Panel | P1 | M | 004 | DONE (executed 2026-09-02) |
| 006  | Add Undo Waypoint and Tap-to-Remove in Measure Mode | P1 | M | 004 | DONE (executed 2026-09-02) |
| 007  | Add Compass / Reset-North Button to Map | P2 | S | — (ideally after 004) | DONE (executed 2026-09-02) |
| 008  | Add Haptic Feedback to All Touch Interactions | P2 | S | — | DONE (executed 2026-09-02) |
| 009  | Redesign Mobile UI to Match Google Maps Layout and UX | P1 | L | — (supersedes 004-005) | DONE (executed 2026-09-02) |
| 010  | Scaffold the Baryar Express + MongoDB API | P1 | M | — | DONE (executed 2026-09-02) |
| 011  | Add Phase 1 Baryar Mongoose models | P1 | L | 010 | DONE (executed 2026-09-03) |
| 012  | Close RFP gaps in `resources/features-roadmap.md` | P1 | M | — | DONE (executed 2026-09-03) |
| 013  | Add `Cargo.transportMode` for V6 land/sea/air/rail/multimodal cargo | P1 | S | 011 | DONE (executed 2026-09-03) |
| 014  | Add phone OTP register/login against the 011 User model | P1 | M | 011 | DONE (executed by pipeline) |
| 015  | Add cargo-owner draft CRUD REST API on the 011 Cargo model | P1 | M | 014 | DONE (executed by pipeline) |
| 016  | Add driver profile + vehicle + documents REST API (driver onboarding) | P1 | M | 014 | DONE (executed by pipeline) |
| 017  | Add matching + offers REST API (driver bidding, owner award) | P1 | M | 015, 016 | DONE (executed by pipeline) |
| 018  | Add shipment lifecycle + events + in-app notifications API | P1 | M | 017 | DONE (executed by pipeline) |
| 019  | Add admin-guarded backend API (users, drivers, cargo, documents, settings) | P1 | M | 014, 016 | DONE (executed by pipeline) |
| 020  | Admin webapp shell — OTP login, layout, sidebar, API client, auth guards | P1 | M | 019, 014 | DONE (executed by pipeline) |
| 021  | Admin users, cargo, drivers, and trips pages | P1 | L | 020 | DONE (executed by pipeline) |
| 022  | Admin verification + settings pages | P1 | M | 020 | DONE (executed by pipeline) |
| 023  | Mobile auth + API client (OTP screens, secure storage, auth state) | P1 | M | 014 | DONE (executed by pipeline) |
| 024  | Mobile cargo-owner flow screens and navigation | P1 | L | 023, 015, 017, 018 | DONE (executed by pipeline) |
| 025  | Mobile driver flow — onboarding, matching, offers, trip management | P1 | L | 023, 016 | DONE (executed by pipeline) |
| 026  | Final MVP test & debug pass | P1 | L | 015–025 | DONE (executed by pipeline) |
| 027  | Add self-service user profile PATCH and public platform settings | P1 | S | 014, 019 | DONE (executed by pipeline) |
| 028  | Tighten matching filters and reject stale offers on cargo cancel | P1 | M | 015, 017, 019 | DONE (executed by pipeline) |
| 029  | Expand in-app notifications and enforce SystemSettings | P1 | M | 018, 019 (soft 028) | DONE (executed by pipeline) |
| 030  | Store driver document bytes on local disk (multer) and serve them | P1 | M | 016, 019 | DONE (executed by pipeline) |
| 031  | Mobile profile edit, public settings, real document upload (clients for 027+030) | P1 | L | 027, 030 | DONE (executed by pipeline) |
| 032  | Mobile notification deep-links + cargo-limit copy (clients for 029) | P1 | M | 029 | DONE (executed by pipeline) |
| 033  | Admin document file preview + maintenance guard (clients for 030+029) | P1 | M | 030, 029 | DONE (executed by pipeline) |
| 034  | Mobile shared constants + navigation types (review cleanup) | P2 | M | 031, 032 | DONE (executed by pipeline) |
| 035  | Backend security hardening — CORS allowlist, JWT pin, error handler, bootstrap fix (review findings #1 #3 #6 #7 #9 #11 #17) | P1 | M | — | DEFERRED (operator 2026-09-05: feature development first — plan file ready, do not execute) |
| 036  | Enforce matching eligibility on offer create (reuse 028 rules) | P1 | S | 028 | DONE (executed by pipeline) |
| 037  | Participant GET /api/shipments/:id/cargo (awarded driver reads params + destinations) | P1 | S | 015, 018 | DONE (executed by pipeline) |
| 038  | Extract shared backend helpers (fail/assertId/pickFields/sendError/requireRole) | P2 | M | 036, 037 (soft) | DONE (executed by pipeline; 253/254 suite green — the 1 failure, driver upload 21, is pre-existing at HEAD and caused by 035's deferred global error handler, A/B stash-verified) |
| 039  | Jest coverage baseline + service branch tests + shared test helpers | P2 | M | 038 (soft) | DONE (executed by pipeline) |
| 040  | Mobile post-login role selection (user / cargo owner / driver) | P1 | M | 023, 024, 025, 031, 032, 034 | DONE (executed by operator session 2026-09-07; `91a7bef` — tsc clean, backend untouched) |
| 041  | User-mode §1 request submit flow — picker reach, draft→publish handoff, EditCargo pickers | P1 | M | 040 | DONE (executed by pipeline) |
| 042  | Google Maps-style LocationPickerScreen — visible marker, search, GPS, address display | P0 | M | 040, 041 | DONE (executed by pipeline 2026-09-07) |
| 043  | ProfileScreen polish — logout confirmation, haptics, design tokens | P0 | S | 042 | DONE (executed by pipeline) |
| 044  | DriverOnboardingScreen — back button, keyboard-avoiding, inline validation | P1 | S | 042 | DONE (executed by pipeline) |
| 045  | OtpVerifyScreen auto-submit + resend timer | P1 | S | 043 | DONE (executed by pipeline) |
| 046  | Driver stack haptic + design token polish | P1 | M | 045 | DONE (executed by pipeline 2026-09-08; `2b29e47` — tsc clean, 5 driver screens: FlatList+haptics+EmptyState+theme tokens; pre-existing lint error in LocationPickerScreen 042 out of scope) |
| 047  | Shared CargoForm component — Create/Edit cargo dedupe | P1 | M | 046 | DONE (executed by pipeline 2026-09-08) |
| 048  | Detail screens pull-to-refresh + haptics, list labels (driver name / cargo title), stack slide animation | P2 | M | 047 | DONE (executed by pipeline 2026-09-08; `4bc3bc8` — tsc clean; plan §4/§5 payload premise corrected: backend sends no `driver`/`cargo` embeds, labels use short-id fallback + owner-side cargo-title join) |
| 049  | Backend: add `transport_company` to User.ROLES + tests (operator P0-A) | P0 | S | — | DONE (executed by pipeline 2026-09-08; 269/270 green — pre-existing driver-upload mime failure unchanged) |
| 050  | Mobile 3-role model (operator P0-B): AppRole, RoleChoiceScreen, ROLE_TABS | P1 | M | 049 | DONE (executed by pipeline 2026-09-08; `68e8511` — tsc clean; AppRole = cargo_owner/driver/transport_company, 'user' removed) |
|| 051  | Transport company placeholder dashboard (operator P0-C) | P1 | M | 050 | DONE (executed by pipeline 2026-09-08; tsc clean; CompanyDashboardScreen + CompanyTab wiring) |
|| 052  | Gate matching and offers on an approved driver profile | P1 | S | 016, 019, 036 | DONE (executed by pipeline 2026-09-08; 275/276 green — pre-existing multer mime test 21 unchanged) |
| 053  | Serialize all admin JSON through existing public* helpers | P1 | S | 019, 038 | DONE (executed by pipeline 2026-09-08; 275/276 green — pre-existing multer mime test 21 unchanged) |
| 054  | Drive matching list queries from vehicleFitsCargo | P2 | S | 028, 036 (soft 052) | DONE (executed by pipeline 2026-09-08; 285/286 green — pre-existing multer mime test 21 unchanged) |
| 055  | Finish test-helper migration, map multer errors, leftover coverage | P2 | M | 038, 039 | DONE (executed by pipeline 2026-09-08; `076f911` — 289/289 green, 24 suites; multer 500→400, auth/sendError swap, pickFields, helper migration, prod log + enrich coverage) |
| 056  | Wire shipment cargo details (037) + design token migration | P1 | M | 037, 034 | TODO |

## Dependency notes (049–055, 2026-09-08 operator model correction + origin2 plan batch)

Operator correction 2026-09-08: the three mobile user models are
`cargo_owner` (sahabe kala — the old "user" role is merged into it),
`driver` (ranande), and `transport_company` (sherkeat hamlo naql), the latter
MISSING and to be added as a first-class mobile user model.

- **049 (P0-A, backend)** = DONE. Add `transport_company` to `User.ROLES` +
  pin with model/admin-filter tests. The duplicate `user` role merge is a
  backend no-op (no `'user'` role exists server-side).
- **050 (P0-B, mobile)**: `AppRole` =
  `'cargo_owner'|'driver'|'transport_company'` ('user' removed), ROLE_META +
  `شرکت حمل و نقل` entry, RoleChoiceScreen 3 cards, ROLE_TABS (company
  reuses CargoTab+ShipmentsTab+NotificationsTab), AuthContext default mapping.
  Client-only; depends on 049.
- **051 (P0-C, mobile)**: mobile placeholder company dashboard
  (profile info + fleet overview placeholder + coming-soon sections). Depends
  on 050. Backend stays untouched (no company API exists).
- Execution order: 049 → 050 → 051 → then 052 → 053 → 054 → 055 (origin2
  backend-quality batch). Backend edits allowed ONLY for 049.

## Dependency notes (031–035, 2026-09-05 client-follow-up batch)

- 035 has **no dependency** on 027–034 and is the highest-leverage open plan (fixes the HIGH CORS finding + crash-prone auth path). Run it first.
- 031 depends on 027 (PATCH /me + GET /settings) and 030 (multipart upload + file GET) being DONE on the backend.
- 032 depends on 029 (offer_* notification types + cargo_limit 409).
- 033 depends on 030 (admin file GET) and 029 (maintenance mode semantics).
- 034 is a pure-refactor cleanup that folds 031/032's new maps in; run after both.
- Recommended order: 035 → (027, 028, 030 any order) → 029 → (031, 032, 033 any order) → 034.

## Dependency notes (040, 2026-09-07 operator directive)

- Operator direction (2026-09-07): focus on MOBILE. First slice = role
  selection after login for the three mobile user models (roadmap §1 user,
  §2 cargo owner, §3 driver). Admin is web-only (§5) — no mobile admin.
- 040 is client-only (mobile/). Depends on 023 (auth context) + the shipped
  024/025/031/032/034 screens. No backend dependency; zero backend changes.
- Recommended next mobile UX slices after 040 (write before executing, in
  order): user-mode §1 polish (request submit flow clarity), then
  per-mode home/dashboard polish. Do not jump to Phase 2.

## Dependency notes (036–039, 2026-09-05 MVP backend gap batch)

- 036 and 037 are independent of each other and of 035. Run them first (any order, or parallel).
- 038 edits the same service/route files 036/037 touch (`matchingService.js`, `shipmentService.js`, `routes/shipments.js`). Soft-depends: land 036+037 first to avoid a merge rewrite of helpers those plans add.
- 039 adds coverage config + shared `backend/test/helpers.js`. Soft-depends on 038 (new util unit tests raise the floor) and should run after 036/037 so the new eligibility + cargo-read tests count toward the threshold.
- 035 remains DEFERRED (operator: feature-first). Do not execute it as part of 036–039.
- Recommended order: (036 ∥ 037) → 038 → 039. Next unwritten id after 039 was 040 (mobile role selection, now DONE).

## Dependency notes (049–052, 2026-09-08 MVP backend leftover batch)

- 049 is the only remaining Phase 1 **product** hole: admin verification exists but does not gate matching/offers. Independent of 047/048 (mobile).
- 050 is independent of 049 (admin serializers only). Can run in parallel with 049.
- 051 edits `matchingService.js` which 049 also touches (`assertApprovedDriver` at the top of list/create). Soft-depends on 049.
- 052 is independent of 049–051 except: if 049 lands first, 052's remaining HTTP-suite migration must `approveDriver` in bid fixtures (do not approve inside `registerDriverViaProfile`).
- 035 remains DEFERRED. 052 maps multer `fileFilter` errors on **one** route; it must not add a global Express error handler.
- Recommended order: (049 ∥ 050) → 051 → 052. Next unwritten id = 053.
- Pipeline: 047 and 048 are mobile TODO and sit **before** 049 in numeric order. If the automation executes lowest-numbered TODO first, 047/048 will run before this backend batch — that is fine (no file overlap).

## Dependency notes

- 005 depends on 004 because it uses the same inset-based layout values (e.g. `CARD_BOTTOM`, `FABS_BOTTOM`) for correct positioning.
- 006 depends on 004 because the undo FAB stacking must be consistent with the bottom offset calculations.
- 007 and 008 are independent of all other plans — they add new features without changing layout.
- Recommended map-era execution order (historical): 004 → 005 → 006 → 007 → 008 → 009.
- 010 has no code dependency on 001–009. It starts the logistics platform next to the existing map apps. Do not edit `mobile/` or `webapp/` in 010.
- 011 depends on 010 because it adds Mongoose models inside the 010 `backend/` tree. Do not add routes, auth, or mobile wiring in 011.
- 012 is docs-only. It does not block 011 (already DONE). Later OTP and cargo-CRUD slices must read the updated modes + OTP bullets in `resources/features-roadmap.md`.
- 013 depends on 011 (`Cargo.js` exists) and on the V6 five-mode bullets. Live checklist is `resources/features-roadmap.md` (V6; `002d41c` renamed it from `features-roadmap-main.md`). Schema-only; no routes.
- 014 depends on 011 (`User.phone` + `phoneVerifiedAt`). It does **not** depend on 013. V6 parked the SMS OTP *gateway* in Phase 2 §9; 014 uses a pluggable sender (dev log + `OTP_FIXED_CODE` for tests) and HS256 JWT in the Authorization header. No cookies, no mobile wiring, no cargo routes.
- 015 depends on 014 (`auth` middleware + Bearer JWT). Owner-scoped CRUD over the 011 `Cargo` model with publish/cancel status exits — no model changes, no matching/offers (017), no shipments (018), no admin (019).
- 016 depends on 014 (Bearer JWT + `User.roles`). OTP verify upserts everyone as `cargo_owner`, so `POST /api/driver/profile` is the only self-service route and grants the `driver` role idempotently; vehicles (globally unique `plate`) and metadata-only document stubs (always `pending`) follow behind a `driver` role gate. No matching (017), no shipments (018), no admin verification (019), no file upload/object store.
- 017 depends on 015 (open/draft cargo lifecycle) and 016 (driver role + owned active vehicles). Drivers list open cargo via `GET /api/matching/cargo` (vehicle capacity + origin `$near` filters), bid via `POST /api/offers`; owners award via `POST /api/offers/:id/accept`, which flips offer → `accepted`, other pending offers → `rejected`, cargo → `matched`. No Shipment (018), no notifications (018), no cancel-time offer rejection (deferred).
- 019 depends on 014 (Bearer JWT auth middleware) and 016 (DriverProfile/Vehicle/Document collections to verify and manage). Admin-only router under `/api/admin` with a `requireAdmin` guard: user list/edit/block-unblock, driver list/detail/profile verification, cargo oversight (edit/cancel with matched-shipment cancel cascade), documents queue + approve/reject, overview counters, `SystemSettings` singleton (get/put). Admin identity via `ADMIN_BOOTSTRAP_PHONES` env seeding (nothing else grants the `admin` role in Phase 1). No admin webapp (020), no user delete, no custom RBAC matrix, no audit log, no companies (Phase 2).
|- 020 depends on 019 (admin API endpoints exist at `/api/admin/*`) and 014 (auth endpoints at `/api/auth/*` + Bearer JWT). Creates the `admin/` Vite app shell with OTP login, sidebar layout, typed API client, and route guards. No data pages (021), no verification/settings (022), no backend changes.
|- 021 depends on 020 (admin shell with routes, API client, auth guards). Adds four data pages: users (table + detail + block/unblock + edit), drivers (table), cargo (table + detail + edit + cancel), and trips (table with enriched shipment data). Adds `GET /api/admin/shipments` backend endpoint. No document verification (022), no settings (022).
- 018 depends on 017 (accepted offer + matched cargo from `acceptOffer` creates the Shipment via `shipmentService.createForAward`). Fixed forward status path assigned→loading→in_transit→(at_customs↔)→delivered→completed (driver-only `POST /api/shipments/:id/status`); driver-only ShipmentEvent logging (cargo_loaded/driver_departed/checkpoint/customs_stop/note, server timestamps); chronological `GET /api/shipments/:id/events`; new Notification collection with in-app-only lifecycle records (shipment_assigned/shipment_status) + `GET /api/notifications` + `PATCH /:id/read`; shipment completion flips cargo matched→completed. No cancel (019 admin), no push/SMS (Phase 2).
- 023 depends on 014 (backend auth API at `/api/auth/*` + Bearer JWT contract). Adds Expo auth plumbing: OTP request/verify screens, expo-secure-store token persistence, typed apiClient with JWT injection, AuthContext provider, and a minimal ProfileScreen. No backend changes. No cargo/driver screens (024, 025).
- 024 depends on 023 (mobile auth + apiClient + AuthContext + navigation shell) and 015/017/018 (cargo CRUD, offers, shipments, notifications APIs all exist). Adds the complete cargo-owner mobile workflow: bottom tab navigation, cargo creation form with MapLibre location picker, draft/published cargo list, cargo detail with publish/cancel/delete, incoming offers list with accept/award, shipment list and detail with event timeline, and notifications screen. No backend changes, no driver screens (025).
- 026 depends on 015–025 (all DONE). This is the test/debug gate slice: run the full backend Jest suite, lint/typecheck/build admin + webapp, lint mobile, fix all failures. No new features.
- 027 depends on 014 (`GET /api/auth/me` + `publicUser`) and 019 (`SystemSettings.supportPhone`). Adds `PATCH /api/auth/me` (name/email/nationalId allowlist) and unauthenticated `GET /api/settings` (platformName/supportPhone/defaultCurrency only). No mobile/admin UI.
- 028 depends on 017 (`listMatchingCargo` + Offer statuses) and 015/019 cancel paths. Filters matching by land/multimodal + volume + reefer↔refrigerated when `vehicleId` is set; rejects leftover `pending` offers on cargo cancel. No notifications (029).
- 029 depends on 018 (Notification collection) and 019 (SystemSettings). Soft-depends on 028 (`rejectPendingOffersForCargo` — inlined in 029 if 028 is still TODO). Adds `offer_received` / `offer_rejected`, optional `shipmentId`, `cargo_limit` on create, and `maintenanceMode` 503 on non-admin writes. No push/SMS.
- 030 depends on 016 (Document stubs) and 019 (admin document queue). Adds multer `POST /api/driver/documents/upload` to local disk + owner/admin file GET. JSON stub POST stays. No S3/GridFS, no mobile multipart client.
- 036 depends on 028 (`listMatchingCargo` filters). Adds `vehicleFitsCargo` and runs it from `createOffer`. List query unchanged.
- 037 depends on 015 (`publicCargo`) and 018 (`getForUser`). Adds `GET /api/shipments/:id/cargo`. Does not open `GET /api/cargo/:id` to drivers.
- 038 soft-depends on 036+037 (same files). Behaviour-preserving extract of fail/assertId/pickFields/sendError/requireRole.
- 039 soft-depends on 038. Adds `test:coverage` + `test/helpers.js` + bootstrap/idempotency/storage tests. Does not execute 035.
- 049 depends on 016 (pending DriverProfile) + 019 (admin verify) + 036 (`createOffer` eligibility). Adds `assertApprovedDriver` on matching list + offer create. Onboarding stays open.
- 050 depends on 019 admin routes + 038 public* helpers. Wraps leftover raw Mongoose JSON. Does not merge serializers (038 rejected that).
- 051 depends on 028/036. Soft-depends on 049 (same file). List query becomes `matchingQueryForVehicle(vehicle)`.
- 052 depends on 038/039. Local multer error map on `/documents/upload`; migrate admin/driver/notifications tests onto `helpers.js`; production-log + listShipmentsAdmin enrich tests. Does not execute 035.

## MVP slice queue (automation source of truth)

This section is the canonical "next plan" queue for the mapapp MVP build pipeline (a cron agent runs every ~10 minutes: if a plan with status TODO exists it executes the lowest-numbered one; otherwise it writes the first queue row that has no plan file yet). Product source: `resources/features-roadmap.md` (V6 Phase 1). RFP: `resources/RFP.pdf`. Write **one plan at a time**. Each slice = exactly one plan file in `plans/` with the next free number. Build order is locked: backend API → admin webapp → mobile app → final test/debug pass.

| Next plan | Scope summary (the plan description) | Depends on |
|-----------|--------------------------------------|------------|
| 014 phone OTP auth | Executed by pipeline (see `plans/014-phone-otp-auth.md`, DONE). | 011 |
| 015 Cargo draft CRUD API (→ 015) | Phase 1 §2: cargo-owner-only REST for the Cargo registry — create/read/list/update/delete draft cargoes with `transportMode` (013 enum), physical dimensions (weight/volume/size), special characteristics (hazardous/fragile/refrigerated), pickup + delivery timing, and origin/destination GeoJSON points accepted as JSON body (no map wiring yet). JWT auth from 014 + `cargo_owner` role checks. No matching, no offers, no admin. | 014 |
| 016 Driver profile + vehicle + documents API (→ 016) | Phase 1 §3: driver onboarding REST — complete/update DriverProfile, register/manage Vehicles (road asset: type, plate, load capacity), and upload Document stubs (vehicle license, driver license, national/professional cards) storing metadata + status `pending` only (no object store yet). JWT + `driver` role. No matching. | 014 |
| 017 Matching + offers API (→ 017) | Phase 1 §2/§3: drivers list open cargoes matching their vehicle type + origin proximity; drivers submit Offer price bids on open cargo; owners list incoming offers for their cargo and select a provider (award). Offer statuses (pending/accepted/rejected) and Cargo status transitions updated accordingly. No GPS. | 015, 016 |
| 018 Shipment lifecycle + events + in-app notifications API (→ 018) | Phase 1 §4/§6: awarding a cargo creates a Shipment; driver manages trip status transitions (started / at_customs / completed + custom checkpoints via ShipmentEvent); status history endpoint per shipment; new Notification collection plus lifecycle-triggered in-app notification records (no push/SMS provider integration). Owner tracking/status endpoints. | 017 |
| 019 Admin backend API (→ 019) | Phase 1 §5: admin-guarded REST — list/manage users and drivers (block/unblock), cargo database oversight (edit/cancel any cargo), transport requests + active trips overview, driver document verification approve/reject, SystemSettings model + get/put endpoints, RBAC via `admin` role guard middleware. | 014, 016 |
| 020 Admin webapp shell (→ 020) | New `admin/` Vite React TS app (do NOT break the `webapp/` map demo): phone OTP login for admin users (JWT bearer in memory/localStorage), app layout + sidebar shell, typed API client, auth guards. | 019 |
| 021 Admin users/cargo/trips pages (→ 021) | Admin screens against the 019 API: users table + detail + block, drivers table, cargo database table + edit/cancel, transport requests overview, active trips monitor. | 020 |
| 022 Admin verification + settings pages (→ 022) | Document verification queue (approve/reject with reasons), system settings form, and basic role management UI against the 019 API. | 020 |
| 023 Mobile auth + API client (→ 023) | Expo: OTP request/verify screens, secure token storage, typed API client, auth state + logout. No cargo/driver screens yet. | 014 |
| 024 Mobile cargo-owner flow (→ 024) | Create cargo using the existing MapLibre origin/destination pickers + draft list/edit + shipment status tracking + offers list/award screens against the 015/017/018 APIs. | 023, 015 |
| 025 Mobile driver flow (→ 025) | Matching open-cargo list, offer submission, active trip status updates + checkpoint logging against the 016/017/018 APIs. | 023, 016 |
| 026 Final MVP test & debug pass (→ 026) | The operator's "test and debug at the end" slice: fix mongodb-memory-server for this WSL machine, run the full backend Jest suite, lint/typecheck/build webapp + admin + mobile, fix every failure found, and produce a written health summary. No new features. | 015–025 |
| 027 user profile PATCH + public settings (→ 027) | Phase 1 §1: `PATCH /api/auth/me` allowlist name/email/nationalId; `GET /api/settings` returns platformName/supportPhone/defaultCurrency (no admin knobs). | 014, 019 |
| 028 matching filters + cancel-offer cleanup (→ 028) | Phase 1 §1/§3: `listMatchingCargo` with vehicleId also filters transportMode land/multimodal, volume, reefer↔refrigerated; owner/admin cancel of open cargo rejects pending offers. | 015, 017, 019 |
| 029 offer notifications + settings enforcement (→ 029) | Phase 1 §1/§6/§5: notify owner on bid and losing/cancelled drivers; enforce maxActiveCargoPerOwner and maintenanceMode. | 018, 019 |
| 030 local document upload (→ 030) | Phase 1 §3 + NFR file storage: multer upload to backend/uploads, server-generated storageKey, owner + admin file download. JSON stubs remain. | 016, 019 |
| 031 mobile profile/settings/upload clients (→ 031) | Mobile: ProfileScreen edit mode (PATCH /me), support block (GET /settings), DriverDocumentsScreen real file pick + multipart upload + authenticated file view. Replaces the dead storageKey text form. | 027, 030 |
| 032 mobile notification deep-links + limit copy (→ 032) | Mobile: tap targets for offer_received/offer_rejected (→ cargo detail), null-safe shipmentId typing, Persian copy map for cargo_limit/validation errors. | 029 |
| 033 admin document preview + maintenance guard (→ 033) | Admin: apiGetBlobUrl + click-to-preview document bytes in new tab, fragment key fix, maintenance-mode confirm + badge. | 030, 029 |
| 034 mobile shared constants + nav types (→ 034) | Mobile refactor: extract STATUS_/MODE_/SPECIAL_/EVENT_ label+color maps and format helpers to utils/constants.ts; typed navigation param lists replacing `as never`. Move-only. | 031, 032 |
| 035 backend security hardening (→ 035) | Review fixes: CORS_ORIGINS allowlist (env-gated, permissive default), HS256 pin, auth DB-error 500 (no crash), global JSON error handler, bootstrap no longer unblocks blocked admins, listDrivers N+1 → aggregate + lean. | — |
| 036 offer matching eligibility (→ 036) | Phase 1 §3: `createOffer` reuses 028 vehicleFitsCargo (land/multimodal, weight, volume, reefer). List filters stay. | 028 |
| 037 shipment cargo read (→ 037) | Phase 1 §3: `GET /api/shipments/:id/cargo` returns `publicCargo` for owner or awarded driver. Owner `GET /api/cargo/:id` stays owner-only. | 015, 018 |
| 038 shared backend helpers (→ 038) | Extract fail/assertId/pickFields/sendError/requireRole. Zero HTTP-contract change. | 036, 037 |
| 039 coverage baseline (→ 039) | `npm run test:coverage` + threshold; shared `test/helpers.js`; bootstrap / notify swallow / storage happy path / createForAward 11000 tests. | 038 |
| 040 mobile role selection (→ 040) | Mobile: post-login role choice screen (user / cargo owner / driver), per-role tab filtering, profile switcher, shipment-list «ثبت درخواست حمل» CTA. Client-only; admin is web-only. | 023, 024, 025 |
| 041 user-mode request submit flow (→ 041) | Mobile (post-040 directive slice 1): register LocationPicker in the Shipment stack so the 040 CTA flow can pick مبدأ/مقصد; draft→publish handoff Alert on create success; origin/destination picker rows + payload in EditCargoScreen. Client-only. | 040 |
| 042 Google Maps-style LocationPickerScreen (→ 042) | Mobile P0: fix invisible marker (ViewAnnotation), add SearchBar, GPS FAB, reverse geocoding address display, haptics, design tokens. Client-only. | 040, 041 |
| 043 ProfileScreen polish (→ 043) | Mobile P0: logout confirmation dialog, hapticWarning on logout, hapticSuccess on save, hapticLight on all Pressables, design token cleanup, card shadows. Client-only. | 042 |
| 044 DriverOnboardingScreen polish (→ 044) | Mobile P1: add back navigation button (currently missing), KeyboardAvoidingView for form inputs, inline validation with real-time error display. Client-only. | 042 |
| 045 OtpVerifyScreen auto-submit + resend timer (→ 045) | Mobile P1: auto-submit when 6 digits entered, 60s countdown resend button replacing static hint, haptic feedback on all actions. Client-only. | 043 |
| 046 Driver stack haptic + design polish (→ 046) | Mobile P1: haptic feedback + EmptyState + design tokens + FlatList for DriverVehicles, DriverDocuments, MatchingCargo, SubmitOffer, DriverOffers screens. Client-only. | 045 |
| 047 Shared CargoForm component (→ 047) | Mobile P1: extract shared CargoForm (origin/destination picker rows, transport chips, dims grid, specials, dates) used by CreateCargoScreen + EditCargoScreen; EditCargoScreen gets theme tokens, haptics, press feedback, KeyboardAvoidingView. Client-only. | 046 |
| 049 backend transport_company role (→ 049) | Operator P0-A (2026-09-08 correction): add `transport_company` to backend `User.ROLES` enum + pin with model/admin-filter tests. The duplicate `user` role merge is a backend no-op (no `'user'` role exists). The ONLY backend change the pipeline may make. | — |
| 050 mobile 3-role model (→ 050) | Operator P0-B: mobile `AppRole` = `'cargo_owner'|'driver'|'transport_company'` ('user' removed/merged), ROLE_META + «شرکت حمل و نقل», RoleChoiceScreen shows 3 cards, ROLE_TABS: company reuses CargoTab+ShipmentsTab+NotificationsTab, AuthContext default role mapping, remove 'user' role handling. Client-only. | 049 |
| 051 transport company dashboard (→ 051) | Operator P0-C: mobile placeholder company dashboard tab — profile info + fleet overview placeholder + 'coming soon' fleet-management sections, so `transport_company` is usable end-to-end. Client-only; no backend company API yet. | 050 |
||||||| 046 Driver stack haptic + design polish (→ 046) | Mobile P1: haptic feedback + EmptyState + design tokens + FlatList for DriverVehicles, DriverDocuments, MatchingCargo, SubmitOffer, DriverOffers screens. Client-only. | 045 |
||||||||| 047 Shared CargoForm component (→ 047) | Mobile P1: extract shared CargoForm (origin/destination picker rows, transport chips, dims grid, specials, dates) used by CreateCargoScreen + EditCargoScreen; EditCargoScreen gets theme tokens, haptics, press feedback, KeyboardAvoidingView. Client-only. | 046 |
||| 048 detail polish sweep (→ 048) | Mobile P2: pull-to-refresh + haptics on detail screens; OffersScreen/ShipmentListScreen show titles not raw ids; stack `slide_from_right`. Client-only. | 047 |
||| 052 driver verification gate (→ 052) | Phase 1 §3/§5: `listMatchingCargo` + `createOffer` 403 `driver_unverified` unless DriverProfile.verificationStatus is approved. Onboarding stays pending-open. | 016, 019, 036 |
||| 053 admin public serializers (→ 053) | Admin GET/PATCH cargo-by-id, cancel, driver detail, verify profile/document return `id` via existing public* helpers (no raw `_id`/`__v`). | 019, 038 |
||| 054 matching query from helper (→ 054) | `listMatchingCargo` vehicle filters come from `matchingQueryForVehicle` (translation of `vehicleFitsCargo`). Unit tests; HTTP contract unchanged. | 028, 036 |
||| 055 test helpers + multer map (→ 055) | Migrate admin/driver/notifications HTTP tests onto `helpers.js`; map multer fileFilter errors on upload only (test 21 → 400); production OTP/notification log tests; listShipmentsAdmin enrich coverage. No global error handler (035). | 038, 039 |

Do not collapse those into 010 or 011. Phase 2 (live GPS stream, payments, KYC, Telegram, companies, ratings) stays parked until Phase 1 booking works.

## Recommended execution order (backend coverage 027–030)

027 (no overlap) can run first and in parallel with 028. 029 should follow 028 when possible (reuses `rejectPendingOffersForCargo`) but does not hard-block if 028 is still TODO. 030 is independent of 027–029 (document upload only). Do not start Phase 2 collections while these four are open.

## Findings considered and rejected

- **RTL panel direction**: The panel opens from the left edge in an RTL app. In RTL, the left edge is the "end" side — this is actually correct for a navigation drawer. The panel toggle uses `chevron-forward`/`chevron-back` icons which adapt to RTL. No fix needed.
- **Bottom sheet migration**: The control panel is a full-height sidebar. The mobile-native pattern would be a bottom sheet (half/full sheet). This is addressed by plan 009 (Google Maps-style redesign) which replaces the sidebar with @gorhom/bottom-sheet.
- **Persistent GPS tracking**: The current GPS is one-shot. Adding `watchPositionAsync` with a toggle is a feature enhancement, not a UI fix. Noted as future work.
- **Neshan API migration**: Replacing Nominatim/OSRM with Neshan for better Iran coverage. Out of scope — current free APIs work. Noted in plan 001 index.
- **Test infrastructure**: No Jest/Vitest configured. Adding tests is a separate initiative. Verification in these plans is via lint + typecheck + manual.
- **Dark mode support**: Significant effort for a color-themed app. Deferred to future work.
- **Orientation lock**: Locking to portrait is common for map apps but not strictly necessary since the layout already works in both orientations. Low priority.
- **Concurrent Promise.all for OSRM routes**: Rejected per plan 001 notes — OSRM public demo server 429s concurrent requests.
- **Stale closure in SearchBar debounce**: The `handleSearch` callback has `[]` deps but receives `value` as a parameter from `onChangeText`, so it's not actually stale. The search works correctly.
- **Phase 2 collections in 011**: Company, Fleet, Staff, Rating, Payment, VehiclePosition, Notification, AuditLog, SystemSettings. The RFP lists them, but Phase 1 booking does not need them. Adding them now would freeze field names before the booking flow exists.
- **Email-first User identity**: Rejected. Iran-first identity is phone (`User.phone` unique). Email is optional profile text. No Firebase uid.
- **Embedding GPS points on Shipment / ShipmentEvent**: Rejected. Live GPS is Phase 2; 011 only allows an optional GeoJSON point on a discrete event (checkpoint / customs).
- **Password / otpHash on User in 011**: Rejected. Auth is the next slice; 011 only reserves `phone` + `phoneVerifiedAt`.
- **OTP / password columns in 013**: Rejected. V6 moved SMS Phone OTP to Phase 2 §9. 013 only adds `Cargo.transportMode`.
- **SMS gateway / Kavenegar / Twilio in 014**: Rejected. V6 Phase 2 §9. 014's sender logs in dev (with code) and uses `OTP_FIXED_CODE` in test; production persists the challenge and logs phone only, no SMS SDK.
- **Cookies + CORS lock-down in 014**: Rejected. 010 left CORS `origin: true` until cookies exist. 014 uses HTTP Bearer JWT so Expo can store a token later without a cookie domain.
- **otpHash / password on User in 014**: Rejected. Challenges live in `OtpChallenge`; User keeps `phone` + `phoneVerifiedAt` only.
- **Separate /register vs /login in 014**: Rejected. Same two endpoints (`request-otp` / `verify-otp`) upsert the cargo_owner user. Profile PATCH is a later slice.
- **Returning the OTP in the JSON body for tests in 014**: Rejected. Tests set `OTP_FIXED_CODE=123456`.
- **Refresh tokens / logout denylist / Redis in 014**: Rejected. One HS256 JWT, 7d. Fail closed if `JWT_SECRET` is missing.
- **Per-mode cargo spec columns in 013** (`containerNumber`, `imoNumber`, `awbNumber`, `wagonId`, Mixed `modeDetails`): Rejected. V6 names five register/manage surfaces; SRS locks extra attributes. Discriminator first.
- **Vehicle types for sea/air/rail in 013**: Rejected. `Vehicle` is a road asset (plate + `vehicleType`). Cargo mode lives on `Cargo`, not `Vehicle`.
- **Phase 2 collections in 013**: Same as 011 — Company, Fleet, Rating, Payment, GPS, Notification, AuditLog stay parked.
- **Copying RFP §5/§8/§9 (SOW, Play Store publish, NDA, SLA) into the feature list (012)**: rejected — contractor process, not app features.
- **Moving GPS / companies / payments into Phase 1 because the RFP lists them in 4.2 (012)**: rejected — RFP 4.6 allows phasing; existing V5 split stays. 012 only ensures they remain named (already true in Phase 2).
- **Planning Phase 2 backend in 027–030 (2026-09-05 coverage audit)**: rejected. Live GPS, companies/fleet, ratings, payments, KYC, Telegram/WhatsApp, SMS gateway, BI, audit logs, permission matrix, Taticom API, and NFRs (pentest, DR, 99.5% SLA) stay parked until Phase 1 booking works. See "Direction (not planned)" in this file.
- **S3/MinIO object store in 030**: rejected for Phase 1. NFR says not-in-Mongo; 010 forbade GridFS; local disk + `storageKey` is the swap point. Cloud store is a later ops plan.
- **Push provider / FCM in 029**: rejected. V6 §6 push engine and §7 provider integration are Phase 2. 018 already left `deliver()` as the swap point.
- **Admin user delete in 027–030**: rejected. 019 deferred it pending cascade rules; still deferred.
- **Custom admin RBAC matrix / zones tables**: rejected as Phase 1 work. 019 implemented a single `admin` role; V6 Phase 2 §4 owns the matrix; zones stay unnamed until product asks.
- **Driver earnings history API**: rejected as Phase 1. V6 §3 names it, but payouts need payments (Phase 2 §9). `Offer.priceRial` on completed shipments is not an earnings ledger.
- **OSRM routing API on the backend (Phase 1 §7)**: rejected. Path calculation already lives in the Expo MapLibre client (plans 001/003/009). Duplicating it in Express would add a public-demo 429 surface the map-era plans already refused. Shipments store no geometry.
- **Stretching Vehicle.vehicleType to ship/plane/wagon so sea/air/rail match (028)**: rejected again (same as 013). 028 hides those modes when a road `vehicleId` is present.
- **Changing JSON POST /documents into multipart (030)**: rejected. Stub POST stays so 016 tests and the current mobile client keep working; upload is a new path.
- **Owner nearby-vehicle search as a new collection (036–039 audit)**: rejected. V6 §1/§2 "searching/viewing transportation options" is implemented as driver matching (`GET /api/matching/cargo`). Vehicle has no GeoJSON point (live GPS is Phase 2). Do not add a reverse geo query.
- **Driver reject-offer HTTP verb (036–039 audit)**: rejected as a new route. V6 §3 "Rejecting transportation requests" is `DELETE /api/offers/:id` (withdraw). Owner award already rejects the other pending bids.
- **Direct booking without a bid (036–039 audit)**: rejected. V6 §1 "Requesting transport services" is the existing offer/accept loop, not a second booking resource.
- **Admin user delete / company registry / RBAC matrix / earnings ledger in 036–039**: rejected again. Same as 027–030: Phase 2 or needs payments.
- **Opening GET /api/cargo/:id to any driver (037 alternative)**: rejected. Would leak unmatched drafts. Participant read is on the shipment.
- **Nesting cargo inside publicShipment (037 alternative)**: rejected. Breaks the mobile shipment type. New `GET /:id/cargo` instead.
- **Global Express error handler / CORS lock in 038–039**: rejected here; that is plan 035 (DEFERRED).
- **Raising coverage threshold to 90% in 039**: rejected. Floor is 70/55/70/70 so a new file does not fail the gate. Ratchet later.
- **Extracting MAX_LIST / merging public* serializers in 038**: rejected. Pagination caps may diverge; DTOs are domain-specific.
- **Per-document completeness gate in 049** (every kind must be approved before bidding): rejected. Phase 1 gate is the existing `DriverProfile.verificationStatus` the admin already sets. A document matrix is a later product call.
- **Blocking profile/vehicle/document CRUD for pending drivers (049)**: rejected. Onboarding must stay open so a pending driver can finish files.
- **JS-side filter after Cargo.find instead of a Mongo query (051)**: rejected. `$near` still needs a query; keep `matchingQueryForVehicle` as Mongo predicates.
- **Hazardous↔tanker or sea-mode vehicles while unifying matching (051)**: rejected again (013/028). Unification only, no new rules.
- **Global Express error handler in 052 so multer 500s die**: rejected here; that is plan 035 (DEFERRED). 052 wraps `upload.single` on `/documents/upload` only.
- **Raising coverageThreshold above 70/55/70/70 in 052**: rejected again (same as 039). Live coverage is already ~90/78/95/92; the floor stays modest so a new file does not fail the gate.
- **Migrating auth.test.js onto helpers (052)**: rejected. That file asserts OTP challenge hash / cooldown side effects; local register stays.
- **Requiring individual Document.verificationStatus on matching (049 alternative)**: rejected. Would 403 every current driver (JSON stubs stay pending; admin verifies the profile, not every kind).

## Direction (not planned) — Phase 2 + client follow-ups

Parked until 049–052 land (last Phase 1 booking API hole + serializer/test leftovers). Do not open these as the next backend plans:

- Phase 2 collections: Company, Fleet, Staff, Rating, Review, Payment, VehiclePosition, AuditLog, KYC.
- SMS gateway replacing `otpService.sendOtp` (V6 Phase 2 §9).
- Push provider replacing `notificationService.deliver` (V6 Phase 2 §6/§7).
- Mobile: DriverShipmentDetailScreen should call 037 `GET /api/shipments/:id/cargo` (origin/destination/dimensions). Not bundled into 037.
- Mobile: map 049 `driver_unverified` to Persian copy (same pattern as 032 `cargo_limit`). Not bundled into 049.
- Admin: document file preview is DONE (033).
- NFRs (encryption-at-rest, pentest, DR backups, 99.5% SLA, a11y) — ops, not API slices.
- Plan 035 security hardening — still DEFERRED (operator: feature-first).
